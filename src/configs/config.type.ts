import { AppConfig } from './app-config.type';

export type AllConfigType = {
  app: AppConfig;
  // database: DatabaseConfig;
  // redis: RedisConfig;
  // auth: AuthConfig;
  // mail: MailConfig;
};
